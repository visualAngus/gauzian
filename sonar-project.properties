# Configuration SonarQube self-hosted - Backend Rust
# projectKey et projectName sont injectés via SONAR_SCANNER_OPTS dans chaque workflow

sonar.sources=src
sonar.exclusions=target/**,migrations/**

sonar.rust.lcov.reportPaths=coverage/lcov.info
