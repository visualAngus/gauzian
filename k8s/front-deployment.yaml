apiVersion: apps/v1
kind: Deployment
metadata:
  name: front
  namespace: gauzian-v2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: front
  template:
    metadata:
      labels:
        app: front
    spec:
      containers:
      - name: front
        image: angusvisual/gauzian-front:dev
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: front
  namespace: gauzian-v2
spec:
  selector:
    app: front
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP
