# ============================================================
# GAUZIAN - Template Secrets Kubernetes
# ============================================================
#
# UTILISATION :
#   1. Copier ce fichier : cp secrets.yaml.example secrets.yaml
#   2. Remplir les vraies valeurs ci-dessous
#   3. Chiffrer : sops --encrypt secrets.yaml > secrets.enc.yaml
#   4. Committer secrets.enc.yaml (NE JAMAIS committer secrets.yaml)
#   5. Supprimer secrets.yaml : rm secrets.yaml
#
# Pour éditer les secrets chiffrés plus tard :
#   sops k8s/secrets.enc.yaml   (ouvre l'éditeur, re-chiffre auto)
#
# ============================================================

apiVersion: v1
kind: Secret
metadata:
  name: gauzian-secrets
  namespace: gauzian-v2
type: Opaque
stringData:
  # ===== BASE DE DONNÉES =====
  DB_USER: "gauzian_user"
  DB_PASSWORD: "REMPLACER_MOT_DE_PASSE_DB"
  DB_NAME: "gauzian_db"
  DATABASE_URL: "postgres://gauzian_user:REMPLACER_MOT_DE_PASSE_DB@postgres:5432/gauzian_db"

  # ===== REDIS =====
  REDIS_PASSWORD: "REMPLACER_MOT_DE_PASSE_REDIS"
  REDIS_URL: "redis://:REMPLACER_MOT_DE_PASSE_REDIS@redis:6379"

  # ===== MINIO / S3 =====
  MINIO_ROOT_USER: "REMPLACER_UTILISATEUR_MINIO"
  MINIO_ROOT_PASSWORD: "REMPLACER_MOT_DE_PASSE_MINIO"
  S3_BUCKET: "gauzian-bucket"

  # ===== SÉCURITÉ =====
  # Générer avec : openssl rand -base64 64
  JWT_SECRET: "REMPLACER_JWT_SECRET_TRES_LONG_ET_ALEATOIRE"

  # ===== SMTP =====
  SMTP_PASSWORD: "REMPLACER_MOT_DE_PASSE_SMTP"
