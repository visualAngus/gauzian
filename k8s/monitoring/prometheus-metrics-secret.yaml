apiVersion: v1
kind: Secret
metadata:
  name: prometheus-metrics-token
  namespace: monitoring
type: Opaque
stringData:
  # Valeur du token — doit correspondre exactement à METRICS_SECRET dans le backend K8s
  # Générer avec : openssl rand -base64 32
  token: "REMPLACE_PAR_TON_METRICS_SECRET"
